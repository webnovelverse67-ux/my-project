# my-project

The project aims to duplicate the website called "Bookme.com" in HTML, css, and javascript, however the premise is to only duplicate the front, with basic HTML, CSS and javascript keeping aspects of the website but not the backend.

All the major components are stored within the folder " Project 2" furthermore the images folder contains some of the images, the video folder can be ignored, the css folder contains the CSS file in which the styles of every page are supposed to go. js is the folder for javascript.

